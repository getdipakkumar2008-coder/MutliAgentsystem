---
description: Show the active plan, gate approvals and recent validation results
---
Run `python "$CLAUDE_PROJECT_DIR/scripts/agentic.py" status` and report: plan status and tier, whether G1/G2 are granted (and G2 scope), and the latest validation results. State plainly what is blocking progress, if anything.
